{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve@14 dist --single --listen $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
